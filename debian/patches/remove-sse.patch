Description: remove flags for compiling with sse-support
 debian policy requires binaries to be i486 compatible, which is not the
 case if compiled with SSE-flags
Author: IOhannes m zmölnig <zmoelnig@iem.at>
Forwarded: not-needed
Last-Update: 2010-08-23
--- a/src/configure.ac
+++ b/src/configure.ac
@@ -153,8 +153,6 @@
  AC_CHECK_CFLAGS([-fPIC])
 fi
 
-AC_CHECK_CFLAGS([-mfpmath=sse -msse])
-
 dnl if we don't have $LD set, we set it to $(CC)
 dnl LD=${LD:=$CC}
 if test "x$LD" = "x"
